# search4clients for Claude Code

Read `AGENTS.md` — it is the canonical instruction file for this project.

## Quick start

When the user asks to find potential clients, run `/search4clients`.

It walks through the full flow: configure request → scan directories → score → export.
